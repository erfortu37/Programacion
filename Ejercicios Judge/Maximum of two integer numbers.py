lista=[]
lista_final=[]
f=0
g=1
a=input()
lista=a.split()
if len(lista)==1:
    while f<g:
        b=input()
        if b.isnumeric():
            lista_2=b.split

for recorrer in lista:
    lista_final.append(int(recorrer))

if len(lista)==1:
    for cont in lista_2:
        lista_final.appen(int(cont))


num_max=max(lista_final)
print(num_max)